type `dskewhyp` <class<`matrix`> | double[], double, double, double, double, double[], logical, double> => (class<`matrix`> | double[]);
type `f` <double[]> => double[] | <double, double> => double | <double> => double;
type `filter` <character[]> => logical[];
type `fn` <double[]> => double | <double> => double;
type `FUN` <double> => integer;
type `logskewhypDens` <double[]> => double[];
type `plot.skewhypFit` <class<`skewhypFit`>, integer[], character[], logical, ...> => null;
type `ppskewhyp` <class<`matrix`>, null, null, null, null, double[], character, character, character, logical, logical, ...> => list<double[]>;
type `print.skewhypFit` <class<`skewhypFit`>, double, ...> => class<`skewhypFit`>;
type `pskewhyp` <class<`matrix`> | double[], double, double, double, double, double[], logical, logical, double, double, logical, ...> => double[];
type `qqskewhyp` <class<`matrix`>, double, double, double, double, double[], character, character, character, logical, logical, ...> => list<double[] | class<`matrix`>>;
type `qskewhyp` <double[], double, double, double, double, double[], logical, logical, character[], double, double, double> => double[];
type `skewhypCalcRange` <double, double, double, double, double[], logical, double, ...> => double[];
type `skewhypCheckPars` <double[]> => list<character>;
type `skewhypDens` <double[]> => double[];
type `skewhypFit` <class<`matrix`> | double[], null, null, character, null | double[], character, logical, logical, logical, any, list<double>, double> => class<`skewhypFit`>;
type `skewhypFitStart` <class<`matrix`> | double[], null, character, null | double[], ...> => list<double[] | character>;
type `skewhypFitStartLA` <double[], null> => list<double[]>;
type `skewhypMode` <double, double, double, double, double[], double> => double;
type `skewhypStepSize` <any, double, double, any, character> => double;
