type `all.identical` <character[] | integer[]> => logical;
type `apply_by_pages` <class<`data.frame`>, class<`function`>, double, logical, ...> => list<any>;
type `asJSON` <class<`data.frame`>, ...> => character[] | <^character[], ...> => character[] | <class<`matrix`>, ...> => character | <^integer[], ...> => ^character[] | <list<any>, ...> => character | <class<`data.frame`> | class<`matrix`> | list<any> | ^character[] | class<`factor`> | class<`character`, `scalar`> | class<`data.frame`, `scalar`> | class<`numeric`, `scalar`> | complex[] | ^double[] | null, ...> => ^character[] | <double[], ...> => character[];
type `as.scalar` <character | class<`data.frame`> | double> => (class<`character`, `scalar`> | class<`data.frame`, `scalar`> | class<`numeric`, `scalar`>);
type `base64_dec` <character> => raw[];
type `base64_enc` <raw[]> => character;
type `cleannames` <character[], logical> => character[];
type `collapse` <character[], logical, integer | null> => character;
type `collapse_object` <character[], character[], null> => character;
type `deparse_vector` <^character[]> => character[];
type `flatten` <class<`data.frame`>, logical> => class<`data.frame`>;
type `fromJSON` <character | class<`json`>, logical, logical, logical, logical, ...> => (^character[] | class<`array`> | class<`data.frame`> | class<`matrix`> | list<any>);
type `givename` <list<any>> => list<any>;
type `is.arraylist` <list<any>> => logical;
type `is_datelist` <list<character> | list<character | null> | list<double> | list<integer | character> | list<integer | null> | list<logical | null> | list<null> | list<null | character> | list<null | logical>> => logical;
type `is.datelist` <class<`data.frame`> | list<any>> => logical;
type `is.matrixlist` <list<any>> => logical;
type `is.namedlist` <character | double> => logical;
type `is.recordlist` <list<any>> => logical;
type `is.scalarlist` <list<any>> => logical;
type `list_to_vec` <list<character | null> | list<null | character> | list<character> | list<integer | character> | list<double> | list<integer | null> | list<logical | null> | list<null | logical> | list<null>> => (^character[] | ^double[] | null);
type `loadpkg` <character> => environment;
type `minify` <class<`json`>> => class<`json`>;
type `null_to_na` <list<character> | list<character | null> | list<double> | list<integer | character> | list<integer | null> | list<logical | null> | list<null | character> | list<null> | list<null | logical>> => (list<character> | list<character | null> | list<double> | list<null | double> | list<integer | null> | list<logical | null> | list<null | character> | list<null> | list<null | logical>);
type `num_to_char` <^double[], double | class<`AsIs`>, null | logical, logical, logical> => ^character[];
type `pack` <^character[] | raw[] | complex[] | ^double[] | null | class<`matrix`> | class<`data.frame`> | list<any>, ...> => (list<class<`character`, `scalar`> | character[] | list<any>> | list<class<`character`, `scalar`> | character | list<any>> | list<class<`character`, `scalar`> | ^character[] | list<any>> | list<class<`character`, `scalar`> | list<any>> | list<class<`character`, `scalar`> | complex[] | list<any>> | list<class<`character`, `scalar`> | double[] | list<any>> | list<class<`character`, `scalar`> | double | list<any>> | list<class<`character`, `scalar`> | ^double[] | list<any>> | list<class<`character`, `scalar`> | integer[] | list<any>> | list<class<`character`, `scalar`> | ^logical[] | list<any>> | list<class<`character`, `scalar`> | null | list<any>> | list<class<`character`, `scalar`> | complex[] | list<list<class<`character`, `scalar`> | integer[] | list<any>>>> | list<class<`character`, `scalar`> | double[] | list<list<class<`character`, `scalar`> | integer[] | list<any>>>> | list<class<`character`, `scalar`> | list<list<class<`character`, `scalar`> | character[] | list<any> | class<`character`, `scalar`> | character | list<any> | class<`character`, `scalar`> | integer[] | list<any> | class<`character`, `scalar`> | double[] | list<any>>>> | list<class<`character`, `scalar`> | list<list<any>>> | list<class<`character`, `scalar`> | list<list<class<`character`, `scalar`> | character[] | list<any> | class<`character`, `scalar`> | complex[] | list<any> | class<`character`, `scalar`> | complex[] | list<list<class<`character`, `scalar`> | integer[] | list<any>>>>>> | list<class<`character`, `scalar`>> | list<class<`character`, `scalar`> | list<list<class<`character`, `scalar`> | character[] | list<any> | class<`character`, `scalar`> | character | list<any> | class<`character`, `scalar`> | double[] | list<any>>>>);
type `parse_and_simplify` <character | class<`connection`, `file`> | class<`json`> | class<`connection`, `curl`>, logical, logical, logical, any, logical, logical, logical, ...> => (^character[] | class<`array`> | class<`data.frame`> | class<`matrix`> | list<any>);
type `parse_con` <class<`connection`, `file`> | class<`connection`, `curl`>, logical> => list<any>;
type `parse_json` <class<`connection`, `file`>, logical, ...> => (class<`data.frame`> | list<list<double | character>>);
type `parseJSON` <character | class<`json`> | class<`connection`, `file`> | class<`connection`, `curl`>, logical> => list<any>;
type `parse_string` <character | class<`json`>, logical> => (list<character | null> | list<list<any>> | list<character> | list<character | list<list<character | list<any>>>> | list<character | list<list<any>>> | list<character | integer | list<any>>);
type `post_process` <list<list<character | integer | list<any>>>, logical, logical, any, logical> => class<`data.frame`>;
type `prettify` <class<`json`>, double> => class<`json`>;
type `rbind_pages` <list<class<`data.frame`>>> => class<`data.frame`>;
type `read_json` <character, logical, ...> => (class<`data.frame`> | list<list<double | character>>);
type `reformat` <character, logical, character> => class<`json`>;
type `row_collapse` <class<`matrix`>, integer> => character[];
type `row_collapse_object` <character[], class<`matrix`>, integer | null> => character[];
type `serializeJSON` <class<`data.frame`> | list<any>, double, logical> => class<`json`>;
type `simplify` <character | integer | null | list<any>, any, any, any, any, logical, any, any, any> => (^character[] | class<`data.frame`> | class<`matrix`> | double[] | ^integer[] | null | class<`array`> | list<any>);
type `simplifyDataFrame` <list<list<any>>, any, any, any> => class<`data.frame`>;
type `stream_in` <class<`connection`, `url`>, null, double, logical, ...> => class<`data.frame`>;
type `stream_out` <class<`data.frame`>, class<`connection`, `terminal`>, double, logical, character, ...> => list<any>;
type `toJSON` <class<`data.frame`> | class<`matrix`> | double | list<any>, character[], character[], character[], character[], character[], character[], character[], character[], any, logical, double | class<`AsIs`>, logical, logical, ...> => class<`json`>;
type `transpose_list` <list<list<any>>, character[]> => list<list<any>>;
type `unbox` <class<`data.frame`> | double> => (class<`data.frame`, `scalar`> | class<`numeric`, `scalar`>);
type `unpack` <list<character | list<any>> | list<character | list<list<character | list<any>>>> | list<character | list<character | list<character | list<any>>>> | list<character | list<double | list<character | list<any>>>> | list<character> | list<character | list<list<character | list<any> | character | list<any> | character>>> | list<character | list<list<character | list<any> | character | list<any> | character | list<character | list<character | list<any>>>>>> | list<character | list<list<any>>>> => (^character[] | class<`data.frame`> | class<`matrix`> | complex[] | ^double[] | null | raw[] | list<any>);
type `unserializeJSON` <class<`json`>> => (class<`data.frame`> | list<any>);
type `validate` <character | class<`json`>> => logical;
type `write_json` <class<`data.frame`>, character, ...> => null;
