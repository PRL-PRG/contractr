type `f` <double[], double[]> => ^double[];
type `filter` <character[]> => logical[];
type `FUN` <character[] | double[], double[]> => null | double[];
type `plot.kernelFunction` <struct<`name`:character, `B`:double, `C`:double>, null, double[], double, character, null> => null;
type `rescale` <^double[], double[], struct<`name`:character>, null> => ^double[];
type `sketch` <^double[], logical, integer, double> => null;
