type `barscale` <any, double, double, character | double[], character> => null;
type `carto.pal` <character, double, character | null, double | null, logical, logical> => character[];
type `carto.pal.info` < > => character[];
type `choro` <class<`units`> | double[], null, character[] | null, double | null, character> => list<character[] | double[]>;
type `choroLayer` <class<`data.frame`, `sf`>, any, any, null, null, character, null, character, double | null, null | character[], character, double> => null;
type `discLayer` <class<`data.frame`, `sf`>, class<`data.frame`, `sf`>, null, character, character, double, double, character, double, double, character, character> => class<`data.frame`, `sf`>;
type `display.carto.all` <double> => list<character | double[] | null>;
type `display.carto.pal` <character> => list<character | double[] | null>;
type `dotDensityLayer` <class<`data.frame`, `sf`>, any, any, null, null, character, double, double, double, character, character, any> => list<list<double>>;
type `filter` <character[]> => logical[];
type `FUN` <integer[]> => (character[] | null);
type `getBorders` <class<`data.frame`, `sf`>, any, any, null> => class<`data.frame`, `sf`>;
type `getBreaks` <class<`units`> | double[], double | null, character, double, logical> => double[];
type `layoutLayer` <character, character, character, logical, character, character, null, null, character, character, logical, logical> => null;
type `legendChoro` <character, character, double, double, double[], character[], double, double, logical, any, any, logical> => null;
type `legendGradLines` <character, character, double, double, double, double[], double[], character, double, logical> => null;
type `legpos` <character, any, any, any, any, double, any, any, any> => list<double>;
