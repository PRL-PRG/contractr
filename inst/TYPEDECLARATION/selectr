type `.subset2` <? environment | character | double | tuple<environment, environment, environment> | list<environment>, ? character | ???, ? environment | double | character, logical> => ? environment | double | character;
type `$` <environment | character[] | language, ? character | environment, double | character> => ? double | environment | character;
type `css_to_xpath` <character, character, character | environment> => character;
type `filter` <character[]> => logical[];
type `first_class_name` <environment> => character;
type `formatNS` <character[]> => character[];
type `formatNSPrefix` <character[], character> => character;
type `fun` <symbol, symbol> => ? list<character | any => any>;
type `FUN` <environment | character> => ? character;
type `is_safe_name` <character> => logical;
type `match_hash` <character> => ^integer[];
type `match_ident` <character> => ^integer[];
type `match_number` <character> => ^integer[];
type `match_whitespace` <character> => ^integer[];
type `parse` <character> => tuple<environment>;
type `parse_selector` <environment> => struct<`result`:environment, `pseudo_element`:null>;
type `parse_selector_group` <environment> => tuple<environment>;
type `parse_simple_selector` <environment, logical> => struct<`result`:environment, `pseudo_element`:null>;
type `querySelector.xml_node` <struct<`node`:null, `doc`:null>, character, ? character, ??? | character> => ? struct<`node`:null, `doc`:null>;
type `querySelector.XMLInternalDocument` <character, ? character, character | ???> => null;
type `querySelectorAll.xml_node` <struct<`node`:null, `doc`:null>, character, ? character[], ??? | character> => tuple<> | tuple<struct<`node`:null, `doc`:null>>;
type `querySelectorAll.XMLInternalDocument` <character, ? character[], ??? | character> => tuple<>;
type `querySelectorAll.XMLInternalNode` <character, ? character[], ??? | character> => tuple<>;
type `querySelectorAllNS.xml_node` <struct<`node`:null, `doc`:null>, character, character[], character, ???> => tuple<struct<`node`:null, `doc`:null>, struct<`node`:null, `doc`:null>>;
type `querySelectorAllNS.XMLInternalDocument` <character, character[], character, ???> => tuple<null, null>;
type `querySelectorNS.xml_node` <struct<`node`:null, `doc`:null>, character, character, character, ???> => struct<`node`:null, `doc`:null>;
type `querySelectorNS.XMLInternalDocument` <character, character, ???> => character;
type `reg_s3_method` <character, character, character, null> => null;
type `sub_simple_escape` <character> => character;
type `sub_unicode_escape` <character> => character;
type `token_equality` <environment, character, character> => logical;
type `tokenize` <character> => tuple<environment, environment, environment> | list<environment>;
type `validateNS` <character[]> => null;
type `xpath_literal` <character> => character;
