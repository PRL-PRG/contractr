type `f` <double[]> => double;
type `filter` <character[]> => logical[];
type `fnm` <double[], null | double> => double;
type `fnmb` <double[]> => double;
type `g` <double[]> => double[];
type `ginv` <double[]> => double[];
