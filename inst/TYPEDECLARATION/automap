type `autofitVariogram` <class<`formula`>, class<`SpatialPointsDataFrame`>, character[], any, ^logical[], logical, null, ^logical[], list<any>, ...> => class<`autofitVariogram`, `list`>;
type `autoKrige` <class<`formula`>, class<`SpatialPointsDataFrame`>, class<`SpatialPixelsDataFrame`>, class<`SpatialPointsDataFrame`>, double, character[], double[], ^logical[], logical, logical, null, ^logical[]> => class<`autoKrige`, `list`>;
type `autoKrige.cv` <class<`formula`>, class<`SpatialPointsDataFrame`>, class<`SpatialPointsDataFrame`>, character, any, ^logical[], logical[], null, ^logical[], list<any>, ...> => class<`autoKrige.cv`>;
type `automapPlot` <class<`SpatialPixelsDataFrame`>, character, any, ...> => class<`trellis`>;
type `filter` <character[]> => logical[];
type `FUN` <class<`data.frame`, `variogramModel`>> => logical;
type `getModel` <double, character, double, double, double, logical, logical, logical, logical> => class<`data.frame`, `variogramModel`>;
type `no_digits` <double> => double;
type `plot.autofitVariogram` <class<`autofitVariogram`, `list`>, logical, ...> => class<`trellis`>;
