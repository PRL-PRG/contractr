type `f` <double, double, integer> => double;
type `filter` <character[]> => logical[];
type `FUN` <double[], logical, symbol | logical, logical> => double[];
type `outlier` <^double[], logical, logical> => double[] | ^logical[];
type `pdixon` <double, integer, double> => double;
type `pgrubbs` <double, integer, double> => double;
type `rm.outlier` <^double[], logical, logical, logical> => ^double[];
type `scores` <double[], character[], ? double, null> => double[];
