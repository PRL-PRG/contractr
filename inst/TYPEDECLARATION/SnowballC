type `filter` <character[]> => logical[];
type `wordStem` <null | character[], character> => null | character[];
type `getStemLanguages` < > => character[];
type `FUN` <character[], character> => character[];
