type `apple_captive_test` < > => logical;
type `filter` <character[]> => logical[];
type `http_get` <character> => character;
type `is_class` <integer> => logical;
type `is_count` <integer> => logical;
type `is_ip_address` <character> => logical;
type `is_string` <character> => logical;
type `is_string_or_null` <? character> => logical;
type `is_type` <integer> => logical;
type `is_up` <character, double, double, logical, logical> => logical;
type `my_ip` <character[]> => character;
type `my_ip_dns` < > => character;
type `my_ip_https` < > => character;
type `nsl` <character, ? character, integer, integer> => struct<`answer`:struct<`name`:character, `class`:integer, `type`:integer, `ttl`:integer, `data`:tuple<character>>, `flags`:logical[]>;
type `ping` <character, logical, logical, integer, double> => double[];
type `ping_os` <character, logical, integer, double> => struct<`cmd`:character[], `regex`:character>;
type `ping_port` <character, double, logical, logical, double, double> => double[];
